New comment to "<%= @post.title %>"
<%= "=" * (@post.title.length + 12) %>

By: <%= @comment.user.name %>

<%= @comment.body %>

View the comment at Bloccit at: <%= topic_post_url(@post.topic,@post,anchor:"comment-#{@comment.id}") %>